---
name: Bug
about: The compiler, a tool or the runtime does something the documentation says it should not
labels: bug
---

The smallest program that shows it:

```cairn
```

The command, and what it printed (`--format json` gives the full record):

```sh
```

What the documentation says should happen, with a link to the section:

The output of `cairn doctor --format json`:

```json
```
