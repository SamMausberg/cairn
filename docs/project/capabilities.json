{
  "profile": "cairn-native/1.0.0",
  "complete_cpp_breadth": false,
  "entire_compiler_proved": false,
  "lean_verified": "collector certificate checker, its 17 certificates, the collector loop model, and a core ownership/lease calculus over locals, record field paths, whole owners, headers, elements, array parts with visible bounds and parallel regions, safety, race freedom between lanes and tasks, and progress for every valuation of those bounds and of the lane count (hand-written, not extracted from checking.py)",
  "model_proficiency_measured": "pilot only: nine function-level tasks, one model family, rule cards and diagnostics as the only documentation, no comparison arm (evidence/v1_1/ai_pilot)",
  "implemented": {
    "native_cpu": "Linux x86-64 and AArch64, C++20 lowering, libraries and executables, clang++ and g++",
    "scalars": "fixed integers (8..64 bit), bool, strict floating point, byte text and character literals",
    "types": "generic records and sums with any value payload, tag-only enums, inline arrays, layout attributes",
    "generics": "generic records, sums and functions with inference and on-demand monomorphization; bounds by trait, kind (copy, affine) and closed scalar class, checked at the call (E-BOUND); templates certified once against their bounds at opaque witnesses or every type of a class (all of std; cairn check --generics)",
    "traits": "static dispatch on Self, multi-trait bounds checked per instance, methods by receiver module, impls held to their trait's declaration, one implementation per type (overlap and ambiguity are errors), borrowed dyn references and owned Dyn[Trait] values served by concrete and generic impls alike; bounded generic impls that apply exactly where their bounds hold; derivable eq, ord and hash",
    "memory": "second-class borrows of values and arrays, checked parts, scoped and first-class zeroed owners",
    "ownership": "affine moves, take/swap, linear structs, defer, implicit release visible as effects",
    "errors": "typed sums with exhaustive match and explicit try propagation",
    "functions": "code pointers and non-escaping closures as borrowed callables; a closure borrows exactly what it captures for the call it is written in",
    "effects": "interprocedural rows with renamed footprints, pure/effects ceilings, mandatory extern effects, unsafe gate",
    "concurrency": "scoped tasks with linear tickets and borrow leases (K-way visibly disjoint parts), atomics with explicit orders, closure-entered mutexes that trap on re-entry",
    "parallel": "race-free lane regions on host threads and CUDA lanes from one body, kernel functions, reductions, device collector; lanes may call fn parameters, judged where the closure is written (lane:f footprint)",
    "placement": "host, pinned, unified and device views in the type; transfer is the only crossing; queued device regions and transfers as linear stream tickets ordered with `after`",
    "modules": "namespaces, pub, imports with aliases and name lists, a packaged std written in CAIRN",
    "targets": "hosted, CUDA through nvcc, and a freestanding AArch64 profile with MMIO and inline target instructions",
    "generation": "library-defined typed recipes over record schemas and naturals (static each/where/require/fold, $ splices, generated records and functions, domains, budgets, content hashes); the packaged std.wire recipe; static families; the certified bounded collector",
    "projects": "ordered files, data-only manifests, fresh build directories; opt-in per-module objects reused by content hash; vendored path dependencies pinned by hash",
    "editing": "host-bound body/expression choices over the whole language, lexical rule cards, effect ceilings",
    "tooling": "comment-preserving formatter, editor grammar, and a language server with diagnostics, hover types and effect rows, outline, completion, signature help, definition into the packaged std, and single-document references and rename",
    "arithmetic_certificates": "17 exact affine implications, checked before every emission and proved in Lean",
    "scalar_equivalence": "trusted translator and Z3 over values (scalars, IEEE f32/f64, records, enums and payload sums with match and try, fixed local arrays), over array views, their parts, rw borrows and function-local heap scratch as SMT arrays with symbolic extents, and over compact, host reduce and loops unrolled within a bounded budget; the observation is the result plus the final contents of every rw parameter, the admitted inputs are the ones the cr::view and cr::disjoint entry guards admit, and explicit per-function/module coverage is required; an observed NaN, a tag inside a view, two views of one array in one call, an owner that moves, an unbounded trip count and anything unmodeled are unknown, never equivalent"
  },
  "not_implemented": [
    "completion-based (non-thread) asynchronous I/O, cancellation and device-loss recovery",
    "multi-device collectives",
    "requiring (rather than reporting) that a program's own templates need only their bounds",
    "SMT coverage of owners that move (take, swap, a returned or stored owner), recursion, concurrency, device placement and unbounded loops",
    "emitter-to-model and native refinement proofs",
    "mechanized ownership and lease rules for single elements, parts of parts, closures, `lane:f` callbacks, device placement, `reduce`/`compact` and queued device work, a proof that the emitter runs a part guard before the task that borrows it starts, and any link from checking.py to the Lean calculus",
    "an equal-budget, multi-language, multi-model AI proficiency experiment with tokenizer accounting",
    "recipes over arbitrary expression fragments (they take naturals, a record type and names of functions), and replaying a checked theorem per recipe instance"
  ],
  "principle": "Implemented, finite-tested, sanitizer-clean, exact-algebra-checked, SMT-equivalent and Lean-checked are distinct scopes."
}
