{
  "profile": "cairn-native/1.0.0",
  "complete_cpp_breadth": false,
  "entire_compiler_proved": false,
  "lean_verified": "collector certificate checker, its 17 certificates, the collector loop model, and a core ownership/lease calculus over locals, record field paths, whole owners, headers, elements, array parts with visible bounds, parallel regions whose lanes own elements or blocks of one stride, and task groups (submissions and collects on any path, wait(g), and what a group holds after a branch join), with single elements, parts of parts and parts whose bounds are not visible modelled conservatively as the elements, safety, race freedom between lanes and tasks, and progress for every valuation of those bounds and of the lane count; the guard-elision rule, whose discharged index, +, -, shift and narrowing guards cannot fail where the facts in scope hold; and the lane pool's region protocol, which runs every index once, returns only when no worker is inside and never waits for a worker to arrive; and the phase rule of a cooperative region, whose accepted phases have no clashing steps in any interleaving and one result whatever order the threads take, and whose blocks of phases have one result (hand-written, not extracted from checking.py; tools/checks/differential_ownership.py requires the two to classify generated programs of a shared fragment identically, and tools/checks/differential_cooperative.py the phase rule and its model to decide generated regions alike); and the layout rule: a declared spread gives every element of its tile one holder and, with a declared storage layout, no two holders write one offset (proofs/Cairn/Layout.lean; tools/checks/differential_layouts.py requires layouts.py and the model to judge generated layouts alike)",
  "model_proficiency_measured": "pilot only: nine function-level tasks, one model family, rule cards and diagnostics as the only documentation, no comparison arm (evidence/v0_8_1/ai_pilot)",
  "implemented": {
    "native_cpu": "Linux x86-64 and AArch64, C++20 lowering, libraries and executables, clang++ and g++",
    "scalars": "fixed integers (8..64 bit), bool, strict floating point with the IEEE-exact builtins sqrt, floor, ceil, trunc, abs, to_bits and from_bits (abs of a signed minimum traps), the storage floats f16, bf16, f8e4m3 and f8e5m2, which never compute and convert only by explicit single roundings, quantize[T](x, scale) and quantize_stochastic[T](x, scale, noise) with their rounding and saturation stated in the receipt, mma_unordered(m, n, k, c, a, b) adding a row-major product of two storage-float matrices into an f32 one, exact products, f32 sums in the hardware's order within (k + 1) * 2^-22 * (|c| + sum |a * b|), the reference loop on the host and 64 x 64 tensor-core tiles with two shared-memory stages and asynchronous 16-byte copies on the device (E-MMA), byte text and character literals; compound assignment (+= -= *= /= %= &= |= ^=) as the checked long form with its place evaluated once",
    "types": "generic records and sums with any value payload, tag-only enums, inline arrays, layout attributes",
    "generics": "generic records, sums and functions with inference and on-demand monomorphization; bounds by trait, kind (copy, affine) and closed scalar class, checked at the call (E-BOUND); templates certified once against their bounds at opaque witnesses or every type of a class (all of std; cairn check --generics)",
    "traits": "static dispatch on Self, multi-trait bounds checked per instance, methods by receiver module, impls held to their trait's declaration, one implementation per type (overlap and ambiguity are errors), borrowed dyn references and owned Dyn[Trait] values served by concrete and generic impls alike; bounded generic impls that apply exactly where their bounds hold; derivable eq, ord and hash",
    "memory": "second-class borrows of values and arrays, checked parts, scoped and first-class zeroed owners, declared field extents (a Buf field naming an earlier usize field of its record, established at construction and held by E-EXTENT-FIELD, so the field goes to a call whole and pays no part guard), extents left out at a call (each usize parameter a later view names as its extent is len of the first view argument that names it, written in by the checker; all or none, never for an extern; E-ARITY), records that lend a view (lends data[0..len];: the record named where an array view is expected is that part, guarded and leased like the written part, and walked by for as its index loop; E-LENDS for a non-Buf field, a bound that is not a literal or usize field, or a second clause)",
    "ownership": "affine moves, take/swap, linear structs, defer, implicit release charged as free where the drop runs",
    "errors": "typed sums with exhaustive match and explicit try propagation; a variant written bare (None, Some(x), Ok(v)) where the context names its sum, checked and emitted as its qualified form, an arm always the subject's, and a visible local, constant, function or type of that name refused as E-VARIANT-AMBIGUOUS; _ as an arm's or a loop's binder binding nothing, an owner payload released where the arm ends and a linear one refused (E-LINEAR-LEAK); an arm that is one return, break, continue, assignment or call written without braces",
    "functions": "code pointers and non-escaping closures as borrowed callables; a closure borrows exactly what it captures for the call it is written in; a call as a statement drops a copyable result and releases an owner there, never an outcome sum try accepts (E-DISCARD) or a linear value (E-LINEAR-LEAK); let _ = e; drops any of those but a linear value on purpose; for x in xs and for i, x in xs over a named view, Buf or Array of copyable elements, elaborated to the index loop and its guards (E-ELEMENT-LOOP otherwise)",
    "implementations": "alternative implementations: fn g(...) implements f when cond needs(feature) { } beside a reference f, with exactly its signature (E-IMPL-SIGNATURE), a row inside its declared ceiling or its own row (E-IMPL-EFFECT), no rounding it does not write (E-IMPL-NUMERICS), a condition over value parameters that cannot trap (E-IMPL-WHEN), declared in its module and never generic, a kernel or an implementation of an implementation (E-IMPLEMENTS), called by name only from test blocks and never reaching its reference (E-IMPL-CALL); plan f use g; runs g where its condition holds and f everywhere else, tested at entry (E-IMPL-USE), and adds the device features the implementation needs to what the build asks of its device target, refusing a target that lacks one (E-IMPL-TARGET); the reference's row joins every implementation's, and the receipt lists each with its condition, what it requires of the machine and its identity, a digest of the two declarations' tokens; an implementation may take naturals, fn g[K:nat](...) implements f when n % K == 0 tune K in [4, 8] { }, each listed value an instance g[8] made and held to every rule whether or not a plan selects it, at most 16 instances, selected with plan f use g[8]; (E-IMPL-PARAM for a natural without values, a value not listed or a selection without values), searched by cairn tune one instance per candidate and validated one instance at a time, its identity the declarations' with its values",
    "effects": "interprocedural rows with renamed footprints, alloc at the acquisition and free at the release, pure/effects ceilings, mandatory extern effects, unsafe gate",
    "machine": "mmio_read, mmio_write and the string asm inside unsafe; typed assembly (asm ptx sm_NN, asm x86_64, asm aarch64): operands numbered outputs first with register constraints from their types (E-ASM-CONSTRAINT, E-ASM-OPERANDS), fresh output locals, host register clobbers from a closed table (E-ASM-CLOBBER), views passed by address under a declared read: or write: that lends them (E-ASM-EFFECT), declared fence, barrier, atomic, io and mmio effects trusted as written, volatile and a memory clobber where an effect is declared or nothing is returned, PTX only in device code with its architecture tested in the device pass and host assembly only on its own host family (E-ASM-TARGET), and typed PTX in a device lane when it declares only reads, writes and fence (E-ASM-LANE); x86-64 assembly runs natively under g++ and clang with the address and undefined sanitizers, PTX compiles for sm_120 and has not run on a GPU, and AArch64 assembly is typed and lowered but has not been built",
    "foreign_implementations": "existing C++ and CUDA as implementations of a CAIRN reference without a rewrite: a manifest's [foreign] table vendors each source and names the symbols it defines, an extern gives each its signature and effects, launch(threads, block) makes one a CUDA kernel host code launches on the thread's stream (E-LAUNCH, E-PLACEMENT), and an implementation whose body is the foreign call stands for the reference under its ceiling; the build compiles each source unchanged with the program's command line and device target and asserts every symbol's C++ types; cairn foreign reports the declared contract (trusted, not checked), native-built, what ptxas reports of each kernel, and validation against the reference with the vendored objects linked under clang++ and g++; a CUDA implementation compiles for sm_120 and its device tests are built and not run outside make gpu",
    "guard_elimination": "a guard is left out of the emitted C++ where the checker established its condition: an index below its extent, a usize + or - that cannot overflow, a conversion that fits; from difference constraints over immutable usize values (loop and lane binders, let, conditions, the left side of && and || for the right, a collector's predicate for its projection, early exits, owner lengths, declared field extents), counted per function as discharged_check_sites (compiler/facts.py); a part x[lo..hi] too where lo <= hi <= len and its extent is hi - lo, and an extent written as a part's own hi - lo; every discharge cites its facts with their origins, verify/elision.py checks each one independently before emission and keeps the guard of any it refuses, and cairn emit --keep-guards writes every guard; a function taking views has a checked C entry cf_f (view and overlap checks) and a lean body ci_f that calls from CAIRN reach, trusted as checked-entries/1; proofs/Cairn/Facts.lean proves a discharged guard cannot fail where the facts in scope hold, and tools/checks/differential_facts.py compares it with facts.py on generated inputs",
    "concurrency": "scoped tasks with linear tickets and borrow leases at the place lent, each on a thread a finished task left parked or a new one, so no spawn waits for a thread and a pipeline starts a thread once (groups keep their berths' threads until wait) (K-way visibly disjoint parts, two fields of one record to two tasks), linear in-place task groups of a declared capacity collected in completion order (spawn f(args) into g; collect(g); wait(g)) whose leases are held until wait, atomics with explicit orders, closure-entered mutexes that trap on re-entry, in-place I/O rings over io_uring (read, write, recv, send and accept in flight from one thread, each moving its Buf[u8] into the ring and back out with the kernel's result, collected in completion order, bounded by timeout operations, stopped by cancel, drained by wait; every submission returned exactly once, a kernel refusal of one submission or of the whole ring (then q.status()) as a -errno value, q.room() and q.pending() ahead of the full-ring and empty-next guards)",
    "parallel": "race-free lane regions on host threads and CUDA lanes from one body, kernel functions, reductions, device collector; lanes may call fn parameters, judged where the closure is written (lane:f footprint); reduce op parallel i in n folds integers on the host lane pool in blocks the count fixes, equal to the in-order fold, floats refused; scan op [exclusive] out for|parallel i in n yield e writes every inclusive or exclusive prefix into out and binds the total, with reduce's operators (checked + on unsigned only, trapping exactly when the in-order total overflows), each yield once and reading out only at its own element, in two passes over reduce's blocks on the lane pool or through CUB on the device, floats only in order (E-SCAN-OP, E-SCAN-ORDER, E-SCAN-TARGET, E-SCAN-EXTENT); std.sort.radix_sort is built on it; a lane may own a block out[b * S .. b * S + S) of a written array for one constant S, directly, through the facts of a loop or condition, or through a part it lends a writing helper; plan f { grain G; lanes L; } sets how f's host regions are split over the lane pool and plan f { block B; per_lane K; unroll U; vector W; } how its device regions launch (whole-warp blocks, indices per thread, unrolled passes, and W adjacent indices a lane runs over one aligned access of each array it touches only at [i], at most 16 bytes, with the scalar lanes when a pointer is off its chunk's width, and stage R, each block loading once into shared memory the elements of an array its lanes only read at [i + d], |d| <= R, and reading them there), and plan f { fuse K; } runs up to K adjacent regions over one extent as one traversal when every array they share is touched only at [i] and no body can trap, loop or be observed, holding a local array only the chain touches in each lane instead of memory, and ending in a host reduce whose fold runs the bodies per step in its own order; no plan changes a result or an effect row",
    "cooperative": "blocks b in G threads t in T { } regions beside parallel: up to three block and three thread names, a runtime grid and a constant block of whole warps (32 to 1024 threads, E-COOP-SHAPE), shared arrays declared in the body and zeroed per block, 48 KiB a block (E-COOP-SHARED), barriers every thread of the block must reach (E-COOP-BARRIER), warp operations shuffle, shuffle_xor, shuffle_down and reduce OP warp yield v in a fixed butterfly order, under conditions whole warps agree on (E-COOP-WARP); a phase rule run for every thread of one block with the block and outside values as symbols, refusing two threads at one shared element between barriers where either writes (E-COOP-CONFLICT, E-COOP-UNORDERED, E-COOP-REUSE) and whatever it cannot decide (E-COOP-UNDECIDED); arrays from outside written by at most one thread of one block, shown by a mixed-radix index over the block, thread and loop names, and read only at the thread's own element (E-COOP-GLOBAL); lowered to one launch with static shared memory, __syncthreads and __shfl_sync (compiles for sm_120, not run on a GPU) or to real host threads meeting at a std::barrier, two blocks at a time, run under the thread sanitizer by the suite; pipeline stages (pipeline p:T[N] depth D;) filled from an outside array by fill, made readable by wait and handed back by release and the next barrier, each stage's state followed through the body at compile time (E-STAGE-UNREADY, E-STAGE-BUSY naming the stage and its outstanding reads, E-STAGE-LOOP), lowered to cp.async with cp.async.wait_group N where N is the fills a wait leaves in flight, so raising D changes the block's shared memory and N and nothing the program restates; double- and triple-buffered row sums agree with a plain reference under the thread sanitizer",
    "placement": "host, pinned, unified and device views in the type; transfer is the only crossing; queued device regions and transfers as linear stream tickets ordered with `after`",
    "modules": "namespaces, pub, imports with aliases and name lists, a packaged std written in CAIRN",
    "targets": "hosted, CUDA through nvcc, and a freestanding AArch64 profile with MMIO and inline target instructions",
    "device_target": "one device target (sm_NNN, with the family suffix f or the arch-specific suffix a) resolved once from --device-target, [build] device_target or the GPU nvidia-smi reports, and passed to the nvcc command, the kernel reader, tune, predict's device card, device timing and the build receipt; E-TARGET for a bad spelling or no target, E-TARGET-TOOLKIT for one nvcc does not compile, E-TARGET-FEATURE for a feature the target lacks (a table of features, each with a probe held to ptxas), E-TARGET-MISMATCH for a result recorded for another target",
    "device_execution": "device regions, transfers, reductions, scans and compactions run on the calling thread's execution context: a region waits for its own stream and not the whole device, temporaries come from one arena that grows to the largest request and is reused, queued work borrows a lane until its wait, and a C caller may hand over its own stream (NAME_device_stream); run on a host machine that counts every stream, allocation and wait, sanitizer-clean under both compilers and thread-sanitizer-clean with two tasks, compiled for sm_120, not run on a GPU; mma_unordered still waits for the whole device",
    "export": "cairn export writes the program a build compiles, exactly the runtime headers it includes and a record of the command toolchain.py gives, the device target, the compilers and versions, a sha256 per file, each function's canonical emission and one identity; build, run and test take the export directory, refuse a changed, added or removed file or an edited record (E-EXPORT-TAMPERED) or another compiler (E-EXPORT-TOOLCHAIN), and write records carrying the identity; --tests and --time export the test program and a timing harness; --compare says whether two exports are the same code and leaves speed to the same harness; the kernels header (device implementation) stands apart from the CAIRN launch wrappers; tested end to end on the host, device exports compiled for sm_120 and never run",
    "generation": "library-defined typed recipes over record schemas and naturals (static each/where/require/fold, $ splices, generated records and functions, domains, budgets, content hashes, declared field extents in generated records); the packaged std.wire recipe; static families; the certified bounded collector; derive grad, reverse-mode differentiation generated as ordinary checked source for immutable lets, returning branches, reduce +, element loops and regions (lanes add into their own element, shared scalars gathered sequentially, cross-lane scatters refused as E-GRAD-RACE) and calls of functions with their own gradient",
    "projects": "ordered files, data-only manifests, fresh build directories; opt-in per-module objects reused by content hash; vendored path dependencies pinned by hash; system libraries only through the toolchain's closed table ([build] libraries, or importing std.zlib); cairn new with a default and four templates (cli, lib, service, parallel), each built, run and tested by the suite; a program of up to 16 MB of source, 32,768 functions and 3,200,000 syntax nodes, and a manifest of up to 1,024 files and 64 vendored dependencies, each limit refused with what it counted; cairn graph prints the module graph with each module's source hash and, with --interfaces, its interface hash; every build writes compile_commands.json; an incremental build of 16 or more units compiles them against a precompiled shared header; Bazel rules (bazel/: cairn_library, cairn_binary, cairn_test) with the examples/bazel workspace",
    "c_header": "cairn build --kind library --header and cairn emit --header: a C header declaring the checked entry cf_ of every function whose types cross (scalars, copyable records, tag-only enums, copyable sums, and borrows of them; a view is a pointer with its length), each with its comment, CAIRN signature and effect row; every size, alignment and offset asserted in the header against the C or C++ compiler and in the library's own C++ against the emitter; the header names its library's interface identity, which only the library built with it defines, so a program built against a stale header fails to link and a stale ctypes binding refuses to load; owners, linear values, function values, dyn, Arrays by value, trait members and kernels listed with the reason instead; cairn emit --ctypes prints a Python module that loads the library with the same layouts asserted against ctypes at import and leaves out, with the reason, align(n) records and by-value packed records and float-holding sums",
    "testing": "test name { } blocks, checked like void functions and held by no other build; assert(cond) and assert(cond, \"why\") trap naming their file and line, and assert_eq(a, b) also prints both scalars (E-ASSERT-EQ for any other type); cairn test runs each block in a process of its own, in parallel and by filter, beside finite JSON contracts, and passes a test only on exit status 0; cairn validate tests an implementation against its reference on boundary inputs generated from its contract (tiles from its condition and body, plan items, the lane pool's cutoff, type edges, views one element off an aligned allocation), each call in a process of its own, traps agreeing only with traps and floats within the host's tolerance, shrinks a failing case and keeps it in regressions/<reference>.json, which cairn test replays against every implementation; Z3's answer is reported apart, and a device implementation's tests run under each Compute Sanitizer tool only in make gpu",
    "editing": "host-bound body/expression choices over the whole language, lexical rule cards, effect ceilings, focused packets that disclose a callee by its interface until the agent expands it, host handles in place of digests (cairn.edit/2), evidence classes for each callee (interface, declared, finite-tested, smt-equivalent, established when a session opens), refusals located in the reply with a computed fix, cairn state and state deltas for a program as it now stands, cairn migrate for one host-authorized signature change through every caller in all files or none, plan edits (cairn.plan/1) that change only a function's plan items, for a function of any module named by its qualified name, and leave every receipt as it was, and cairn explain: guards, allocations, waits and vectorizer verdicts per .cairn line, with each cooperative region's shared memory, barriers, pipeline waits and warp collectives at their lines, and implementation sessions (cairn.implementation/1) that pin a reference, its tolerance, test policy and permitted inputs by digest, refuse a submission reaching for any of them (E-REFERENCE, E-TOLERANCE, E-TEST-POLICY, E-DOMAIN), admit only an implementation that validates (E-VALIDATION with its shrunk input otherwise), and hand every submission to a candidate-history callback, and cairn mcp, which serves check, state and the edit, plan and implementation hosts over the Model Context Protocol (stdio, JSON-RPC 2.0, eight tools) and writes an admitted change back to its files only while every file holds what the host judged it against (E-SESSION otherwise)",
    "tooling": "comment-preserving formatter; TextMate and Vim grammars generated from the compiler's vocabulary and checked against a TextMate engine; a language server that analyses a document with its whole project (diagnostics, semantic tokens, inlay hints for effect rows, omitted extents and let types, hover with doc comments, definition into the packaged std, completion, signature help, document and workspace symbols, highlights of reads and writes, run and run-test code lenses, quick fixes that choose nothing, and project-wide references and a rename admitted only when every receipt but the name is unchanged, which follow a function into implements and plan f use g); a VS Code extension; rendered refusals and one-line results at a terminal beside the JSON record; cairn check --watch; bash and zsh completion scripts generated from the command line's parser; clangd settings for the runtime headers",
    "performance_prediction": "cairn predict: per function a time at given sizes, a piecewise formula in the first extent, the bound of each part, a fraction of the machine's speed of light and a confidence with its reasons, from work counted in the extents (operations, streams, data-dependent accesses, regions, tasks, allocations) and a machine profile (cairn.perf.calibrate on the host; the device profile is a published specification); --against prices a change without building it; validated against held-out timings on one machine (evidence/v1_0/perf_model), weakest for wide host regions of 1e5 to 1e7 elements; llvm-mca loop cycles and ptxas/cuobjdump kernel resources read at compile time; cooperative regions priced by their blocks from each thread's work between barriers as its warp runs it (the phase rule's run of one block gives the lanes, banks and sectors of every access and branch), the blocks an SM holds by threads, registers (with --inspect, from ptxas) and shared memory, and a pipeline's copies at the bytes its stages keep in flight over an assumed latency, so its depth changes shared memory, occupancy and the copy rate; each figure labelled checked, ptxas, specification or assumed",
    "performance_search": "cairn tune: a search over every combination of a function's plan items, stage and vector beside fuse included, each complete candidate checked as a whole program and refused ones counted with the checker's code; device candidates compiled in predicted order for ptxas and cuobjdump resources (registers, spills, stack, static and staged shared memory, instructions), each kept by the digest of what it compiled and its target, with the SASS digest that shows two plans compile to the same code; the function's implementations as candidates beside its plans, chosen or timed only while a validation of each holds, a cooperative region's block shape and pipeline depth searched as an implementation's natural parameters; explicit budgets of compiles, seconds and timed runs, with what a spent budget left undone counted; parallel regions named by a digest of their syntax so an unrelated edit keeps the name; --compare, a difference report whose every line is a compiler observation, runtime measurement, profiler observation, hypothesis or suggested experiment; a candidate history (.cairn/history) of attempts, failures, validations, observations, measurements with their procedure, profiles, hypotheses and experiments under each candidate's identity (source, contract, target, compiler, artifact), stale records never shown as current; and cairn state --symbol, one function's investigation from what still holds, with deltas",
    "arithmetic_certificates": "17 exact affine implications, checked before every emission and proved in Lean",
    "scalar_equivalence": "trusted translator and Z3 over values (scalars, IEEE f32/f64, records, enums and payload sums with match and try, fixed local arrays), over array views, their parts, rw borrows and function-local heap scratch as SMT arrays with symbolic extents (two views of one array in one call share that array, and the callee's disjointness guard is modeled as emitted), over owners that move (take and swap on locals, an owner passed by value with a free length, an owner returned and observed by its length and elements), and over compact, host reduce and loops unrolled within a bounded budget; the observation is the result plus the final contents of every rw parameter, the admitted inputs are the ones the cr::view and cr::disjoint entry guards admit, the admitted inputs are exactly what the emitted entry guards admit, so the top-level tag of an enum or sum passed by value names a declared variant while a tag nested in a record, an array or a payload, reached through a borrow or held in storage carries any value, a match over one that names no variant aborts as the emitted default does, and a try over one is unknown, and explicit per-function/module coverage is required, with an optional per-function precondition (cairn verify --assume symbol=expression) recorded in the receipt and named in the domain it quantifies over; an observed NaN, an owner inside a record, a sum or an array, an unbounded trip count and anything unmodeled are unknown, never equivalent",
    "version_diffs": "cairn diff OLD NEW over paths or revisions of the repository, read object by object without a checkout (and --std for the packaged library): one class per function, identical-code (emitted C++ equal up to renaming, for it and everything it reaches), identical-source (a template no code instantiates, compared by its tokens and what it names), smt-equivalent (the value fragment above), behavior-changed with a witness the value model and native builds under each present compiler replay, or unknown with its reason, never counted as unchanged; signature-changed, added, removed and renamed; exact deltas of signature, effect row, guard sites, emitted guards, allocations, local storage, tasks and unsafe blocks; a predicted cost change labelled predicted; a semantic-version verdict over the public interface with its reasons and its unproven functions; --require identical|equivalent as a gate; a Markdown section for a pull request written to a file; and the edit contract preserve: identical|equivalent, refused as E-PRESERVE with the witness",
    "printing": "print, println, eprint and eprintln write integers, bools, character literals, shortest round-trip floats and the bytes of u8 views and Vec[u8] in one call, every argument computed left to right before a byte is written, through one 4096-byte stack buffer and no allocation; format appends the same text to a Vec[u8]",
    "layouts": "layout declarations evaluated by the checker: storage layouts rows, cols, strided, pad, swizzle (CuTe's Swizzle<B, M, S>), transpose and tile, spreads that give participants blocks of a tile, and inverse, which names the holder of each element; in code L.at(r, c), D.row(t, v), D.col(t, v) and D.at(t, v) with each argument checked, and the constants size, cosize, extent, participants and values; every storage layout gives each element its own offset and every spread each element exactly one holder (E-LAYOUT-OVERLAP, E-LAYOUT-GAP), a spread read through a layout of another shape is E-LAYOUT-CONSUMER; the receipt and cairn explain report coverage, the participant holding each element, runs of adjacent values and shared-memory bank conflicts, all by enumeration up to 262,144 elements",
    "fragments": "tensor-core fragments WmmaA, WmmaB, WmmaAcc (nvcuda::wmma) and MmaA, MmaB, MmaAcc (PTX mma.sync m16n8k16) of f16 or bf16 accumulating in f32: fill, mma_load[F](tile, L, i, j) and mma_store(tile, L, i, j, acc) through a layout, acc = mma_unordered(acc, a, b) under the whole multiply's contract, and mma_get and mma_set on the elements a lane holds of an mma.sync accumulator, whose share is the PTX ISA's (Lean-checked against the formula); each a warp operation, legal only inside a cooperative region (E-FRAGMENT), its coordinates, fill value, A, B and a WMMA accumulator the same in every thread of a warp (E-COOP-WARP); a tile whose length is not a literal is checked against the layout's cosize where the operation runs; a WMMA store names no lane, so the phase rule takes it as the warp's write; a layout a family cannot read is E-LAYOUT-CONSUMER; each family is a device capability of the build's target (wmma, mma_sync, bf16), and TmemAcc, which needs tcgen05, is refused (E-TARGET-FEATURE); on the host a warp's threads hold each fragment whole and store their lane's elements, tested as 32 threads under the thread sanitizer; the device operations compile for sm_120 and have not run on a GPU; two matrix multiplies written with them in examples/tensor, 64 x 64 WMMA tiles over two stages and 64 x 32 mma.sync tiles over a swizzled stage, each within mma_unordered's bound and bit-equal to its reference loop on the host under both compilers and the thread sanitizer"
  },
  "not_implemented": [
    "cancellation of tasks, and device-loss recovery",
    "multi-device collectives",
    "requiring (rather than reporting) that a program's own templates need only their bounds",
    "SMT coverage of an owner held inside a record, a sum or an array, recursion, concurrency, device placement, storage floats and quantization, a function that asserts, and unbounded loops without a precondition",
    "emitter-to-model and native refinement proofs",
    "mechanized ownership and lease rules for closures, `lane:f` callbacks, device placement, `reduce`/`compact`, queued device work, a task group submitted to inside a loop, and declared field extents (single elements, parts of parts and invisible bounds are conservatively `elems` in the calculus, regression-pinned), a proof that the emitter runs a part guard before the task that borrows it starts and that cairn_parallel.hpp performs the proved region protocol under its memory orders, and a proof-level correspondence from checking.py to the calculus (the differential harness tests agreement on generated programs; it proves nothing)",
    "an equal-budget AI experiment where the languages differ in tasks solved: the preregistered run on ten small tasks with one model solved every task in CAIRN, C++ and Rust at 11.6 times the tokens in CAIRN, so larger programs and other model families remain",
    "writing the minimum of a signed type as a literal (E-LITERAL-RANGE for -9223372036854775808 and for a constant 0 - MAX - 1)",
    "recipes over arbitrary expression fragments (they take naturals, a record type and names of functions), and replaying a checked theorem per recipe instance",
    "extent identity through an element of an array of records (cs[0].price is still a part), and full independence of alloc and free in effect rows (a constructor row still carries free)",
    "plan items that transform code beyond fusion, device vector chunks and shared-memory stencil tiles (host tiling), fusion of bodies that can trap, and plans for queued device regions",
    "device validation: the device half of the performance model is a published specification, and device plans, the execution context, the tensor-core multiply and the storage floats, gradients and asserts in a lane compile for sm_120 but run only under the owner's make gpu, make tune-device and make calibrate-device, which have not run them",
    "an owner crossing the C boundary (no generated release function for a Buf), two CAIRN headers in one translation unit (their ct_ type names collide), and the header and ctypes binding on any platform but x86-64 Linux",
    "checking one module on its own: the checker judges the whole program (effect rows are a fixed point over the call graph, and ceilings, operand order and lane and device reach read them), so every check, editor refresh and incremental build runs the front end over every module; a hermetic Bazel toolchain and one Bazel action per module"
  ],
  "principle": "Implemented, finite-tested, sanitizer-clean, exact-algebra-checked, SMT-equivalent and Lean-checked are distinct scopes."
}
