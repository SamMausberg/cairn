# An adversarial review of the implementation layer

On 2026-09-23 one reviewer tried to break what 0.9 added after `754f0a0`: cooperative regions and pipeline stages, layouts and tensor-core fragments, implementations with their dispatch and validation, typed assembly and foreign implementations, the device target, `cairn export`, plan sessions on named modules, the candidate history, `cairn tune --write` and `cairn mcp`. Three helper agents wrote and ran attacks in parallel; the reviewer ran every defect below again before counting it, except where the table says a helper's run is the only one. [SECURITY.md](../../../SECURITY.md) defines a soundness bug.

The attacks were CAIRN programs, crafted project and export directories, and scripted host requests, checked by main from `3660e8e` on. Every accepted program that makes a claim about its run was built with the runtime headers and run under ThreadSanitizer, or under AddressSanitizer with UndefinedBehaviorSanitizer, with clang++ 21.1.8 and g++ 13.3, on one x86-64 machine shared with other agents. Nothing ran on a device. Typed PTX has no host lowering, so attacks through it are checked for acceptance only.

## Fixed

Twenty-one defects were found, and all are fixed on main. Each fix is in the file that owns the rule, and `tests/soundness/test_review_implementation_layer.py` keeps the program or request that showed it, except those fixed by another track: PARAMS's in `tests/language/test_implementations.py`, the MCP fix in `tests/agent/test_mcp.py`, TENSOR's in `tests/soundness/test_fragments.py` and `tests/language/test_layouts.py`, and PROOFS's in `tests/soundness/test_reach.py`, `test_pipelines.py` and `test_cooperative_faults.py`, which takes each refusal back out and shows the hang, the race or the crash. Where the owning track was still changing the file, the reviewer sent it the program and it made the fix.

| defect | what went wrong | fix |
|---|---|---|
| An export record ran any command | The identity is a digest anyone can recompute. An `export.json` whose command was `sh -c ...`, with the identity recomputed, was run by `cairn build DIR` and reported `native-built`; so was a "compiler" the export shipped, whose `--version` ran first. | `87272e8`: a build runs only the command `toolchain.py` gives for the record's kind, architecture, target and libraries, with clang++ or g++ and nvcc found on `PATH` by name (`E-EXPORT-TAMPERED`, `E-EXPORT-TOOLCHAIN`) |
| A validation outlived a changed helper | An implementation's identity digests the two declarations as written. After the helper `settle` changed to add one, `cairn validate` failed at `n = 0` with the same identity, and `cairn tune --write` still cited the old validation and wrote `plan total use total_fast;`. | `483fa53`: a validation and every candidate that selects an implementation are kept under the implementation and everything it calls as lowered |
| A validation outlived a changed vendored source | The lowered code of a foreign implementation is the call of its extern. After `vendor/histogram.cpp` added one to every bin, validation failed and `cairn tune` still chose the implementation. `cairn state --symbol` showed any validation as current while the reference was unchanged, since `judged` rebuilds a record's source from the variant it stored. | `e65e708`, `69a1eb9`: the identity adds the sha256 of each vendored source the row reaches, and the investigation packet compares each record's implementation with the one the program holds now |
| An implementation could print its own validation | Each call ran in a fork that kept the child's standard output, which carries the answers to the validator. An implementation printing `{"outcome": "return", ...}` lines had them read as every later call's answer, so one returning 12345 for `x` passed; one printing `hello` broke the run with a `JSONDecodeError`. | `103661b`: each call runs with standard input and output on the null device |
| `-0.0` was never tested and equalled `0.0` | `edges()` listed `-0.0`, and `dict.fromkeys` dropped it as a duplicate of `0.0`; the comparison also took them as equal at zero tolerance. `x + 0.0` against `x` passed while Z3 in the same record gave `x = -0.0`. | `563d422`: `-0.0` is its own edge, and differs from `0.0` unless the policy gives a tolerance |
| An empty replay passed | `cairn test` replaying a regressions file whose reference had no implementation left, or had been renamed, ran nothing and said `passed-finite-tests`. | `63ad9b7`: it answers `unknown` with the reason |
| The phase rule kept a local a callee rewrote | `let mut k = t; put(k, 0); s[k] = u64(t);` and the same through `swap` were accepted, the rule still reading `k` as `t`; ThreadSanitizer reported a race under both compilers. A closure passed to a call that assigns `k = 0` did the same (not run). | `827b4db`: a local lent `rw` to a call, `swap` or `take`, or written by a closure, is unknown afterwards (`E-COOP-UNDECIDED`) |
| The phase rule dropped an arm after `&&` | For `if x[0] == 7 && t - 5 > 100 { } else { s[max(t, 4)] = u64(t); }` the right side traps for threads 0 to 4, and the rule treated the whole condition as a trap, recording neither arm; at run time `x[0] != 7` skips the right side, threads 0 to 4 all write `s[4]`, and ThreadSanitizer reported the race under both compilers. | `827b4db`: an unknown left side and a trapping right side give what the left side alone decides (`E-COOP-CONFLICT`) |
| The phase rule ignored typed PTX | `asm ptx sm_75 "st.shared.u32 [%0], %1;" (sc, 7) effects(write:sc);` in every thread, or in one thread beside another's read, was accepted, though an address reaches the whole array. Acceptance only. | `827b4db`: a declared `read:x` or `write:x` of a shared array is a whole-array access of it (`E-COOP-CONFLICT`, `E-COOP-UNORDERED`) |
| `cairn mcp` wrote outside its directory | A session opened on an absolute path or `../other` wrote an admitted edit into that project. | `a52726b` (AGENT-TOOLS): a path outside the served directory is `E-REQUEST` |
| The direct dispatch ignored float rounding | `f(X)` with `const X:f32 = -0.1` called `g ... when x < -0.1` directly: the fold compared in exact arithmetic, the entry in f32, where the condition is false. The program returned 7.0 against -0.1 under both compilers; `when x < 16777217.0` with `f(16777216)` did the same. | `ac93762` (PARAMS): a condition that computes with a float is tested on entry |
| `E-IMPL-CALL` missed a cycle through two references | `f2 implements f = k(n)` and `k2 implements k = f(n)`, both selected, were accepted, and `f(3)` recursed until AddressSanitizer reported a stack overflow (g++; clang++ exited 56). | `ac93762` (PARAMS): the cycle check runs again once the dispatch edges are in |
| A fragment tile had no run-time bound | A load or store through a view whose extent is not a literal, `a:ro<f16>[n]@unified` with `n = 16` and a 16 x 16 layout, was a heap-buffer-overflow in `cr::frag::load` under both compilers. | `970328e` (TENSOR): `cr::layout::holding` traps when the view holds fewer elements than the layout's cosize, on the host and in device lanes; the program now stops at the guard (exit -6) with no sanitizer report |
| Layout offsets wrapped past 2^64 | `strided(3, 1, 2^63, 1)` passed the distinctness check while element (2, 0) landed on offset 0 in C++, and two threads writing through it raced; `swizzle(rows(32, 32), 3, 4, 64)` shifted by 64. | `970328e` (TENSOR): `E-LAYOUT` for an offset of 2^63 - 1 or more, and for a swizzle with `B + M + S` over 63 |
| Fragment arguments could differ within a warp | WMMA loads and stores at `(t % 2, 0)`, `WmmaAcc(f32(t))` and an `MmaA` load at `t % 2` were accepted; WMMA leaves them undefined on the device, and the host emulation answered per lane. | `970328e` (TENSOR): `E-COOP-WARP` for a thread-level coordinate, fill value, operand or WMMA accumulator |
| A device fragment load ran on host threads | A region whose only device view was read through `mma_load` was placed on the host, where it dereferenced the device pointer. | `970328e` (TENSOR): a fragment's view places the region (`E-PLACEMENT`) |
| A WMMA store named a writer lane WMMA does not | The phase rule took a WMMA store's writer from the `mma.sync` share, so a thread reading its own lane's elements after a store, with no barrier, was accepted. | `970328e` (TENSOR): a WMMA store is one write by its warp with no lane named (`E-COOP-UNORDERED`); an `mma.sync` store keeps the ISA's share |
| A barrier under a per-thread local was accepted | A barrier under a local set through an `rw` borrow (`put(k, t); if k == 0 { barrier; }`), by a closure, or through an array element hung on host threads under ThreadSanitizer with both compilers, where an unconditional barrier exits 0. A chain of eight moves in a loop was accepted where seven was refused (a helper's run). | `1ac8dc2` (PROOFS): the thread-level analysis runs loops to their fixed point and raises a local lent `rw`, written through an element or assigned in a closure (`E-COOP-BARRIER`) |
| A closure raced in a cooperative region | A closure that writes `out[0] = u64(t)` or `s[0] = u64(t)`, passed to a call in every thread, was accepted and raced under both compilers; `parallel` refuses the same with `E-PARALLEL-RACE`. | `1ac8dc2` (PROOFS): a closure naming a shared array, a pipeline or an outside array is `E-COOP-UNDECIDED` |
| A stage was read before its wait | A read through anything but `tiles[i]`, as `first(64, tiles)` or `first(tiles[0..4])`, dereferenced the null stage pointer (AddressSanitizer SEGV under g++, unknown-crash under clang++); a closure capture and a fill from a pipeline never waited on crashed the same way (a helper's runs). | `1ac8dc2` (PROOFS): any use of the name but as the receiver of its own operations reads the readable stage (`E-STAGE-UNREADY`) |
| A fill's source placement was not checked | A fill from a `@device` source in a host region, or from a host view in a device region, was accepted. | `1ac8dc2` (PROOFS): a fill's source is an outside array in the region's placement (`E-PLACEMENT`) |

## What held

`tests/soundness/test_review_implementation_layer.py` keeps 78 attacks that were correctly refused, each with the code it must keep. What was tried, by target:

| target | tried | outcome |
|---|---|---|
| `when` | checked `+ - *`, negation, narrowing and float-to-integer casts, `/` and `%` by a zero literal, constant or hex zero, `shr` by the width, `len` of an owner, calls, indexing, an `rw` borrow, a field, `abs`, a function parameter, a layout's `at`, a condition false for every input | `E-IMPL-WHEN` |
| implementation rules | reaching the reference through a helper or a function value, calling an implementation by name or as a value, implementations of implementations, generics, externs, kernels and other modules, renamed parameters, `rw` for `ro`, another placement or extent name, allocation, lanes, printing, assembly and possible divergence beyond the ceiling, a lane calling a reference whose implementation uses lanes or allocates, selecting another function's implementation, two selections, the reference itself | `E-IMPL-CALL`, `E-IMPLEMENTS`, `E-IMPL-SIGNATURE`, `E-IMPL-EFFECT`, `E-PARALLEL-CALL`, `E-IMPL-USE` |
| validation | an implementation that traps where the reference wraps, and the reverse; a wrong `rw` view; an unsatisfiable `when`; an `rw` scalar; a difference only at `n = 1234` | failed with a shrunk input, failed, `unknown`, `unknown`, and a pass labelled finite-tested with Z3's answer bounded to `n <= 16`, the finite-testing miss the label states |
| fragments | loads under `t % 2`, a data condition, a copy of `t`, a per-thread loop or break; two warps storing one fragment; a store beside another warp's read or load; a fragment made outside a region, in a lane, stored into or loaded from a device `rw` view; a layout hidden by a local or past a constant-length array | `E-COOP-WARP`, `E-COOP-CONFLICT`, `E-COOP-UNORDERED`, `E-COOP-REUSE`, `E-FRAGMENT`, `E-LAYOUT-CONSUMER` |
| layouts | the lowered `L.at`, `D.row`, `D.col` and `D.at` of 120 random accepted layouts (rows, cols, strided, pad, swizzle, transpose, tile, spreads) against the checker's own enumeration, under both compilers with UndefinedBehaviorSanitizer (a helper's run) | no mismatch |
| typed assembly | PTX writing an array lanes write, writing an `ro` view, an address with no declared effect, `io` in a lane, `barrier` in a cooperative thread, x86-64 in a device lane, host assembly writing through an address in a host lane, `%10` and `%[x]`, `memory` and `rsp` clobbers, a `u8` PTX operand, a leased owner, one array through two operands, a host view from a device lane, an effect naming no operand | `E-PARALLEL-RACE`, `E-WRITE-LEASE`, `E-ASM-EFFECT`, `E-ASM-LANE`, `E-ASM-TARGET`, `E-PARALLEL-CALL`, `E-ASM-OPERANDS`, `E-ASM-CLOBBER`, `E-ASM-CONSTRAINT`, `E-LEASED`, `E-ALIAS`, `E-PLACEMENT` |
| cooperative regions | a `return` in a region, an element loop over a stage before any wait, a barrier under a local assigned from `t` through seven moves, an outside write through a local lent to a call | `E-PARALLEL-CONTROL`, `E-STAGE-UNREADY`, `E-COOP-BARRIER`, `E-COOP-GLOBAL` |
| `cairn export` | a changed byte, an added or removed file, an edited record, a listed name that is a directory; after the fix, a recomputed identity over a shell command, a flag the toolchain never gives, a shipped compiler and an artifact path outside the build | `E-EXPORT-TAMPERED` or `E-EXPORT-TOOLCHAIN`, and nothing ran |
| `cairn mcp` | a file saved between opening a session and its request, a reply to a spent plan session, a plan on `lib.spread` | `E-SESSION` with nothing written; the plan lands after its declaration in `src/lib.cairn` |
| plan sessions, `tune --write` | reply items outside `PLAN_ITEMS`; an existing `plan f use g;` beside a new plan; a CRLF or byte-order-marked file | refused, kept, and refused without writing (below) |
| foreign manifests | read, not run: `[foreign]` paths go through `contained_file`, which refuses absolute paths, `..` and symbolic links, and symbols must be C identifiers | held on reading |

## False refusals

None lets anything unsafe through, and none was changed. `cairn tune --write` refuses a file with CRLF line endings (`E-DECLARATION` from the recheck, since offsets into the combined source miss the carriage returns) and one that starts with a byte-order mark (the function "is not declared in a file of this project"), writing nothing in both cases. An implementation that calls itself recursively is `E-IMPL-CALL`. `effects(...)` after `implements f` is `E-PARSE`; it must come first. An asm address of a record field, `(s.b)`, is `E-PARSE`. A layout whose cosize passes `usize` is `E-LITERAL-RANGE`, which names the literal rather than the layout.

## What this does not show

One reviewer and three helpers wrote every attack, on one x86-64 machine, and nothing ran on a device: the device lowering of cooperative regions, fragments, pipelines and typed PTX is compiled at most, and typed PTX attacks were checked for acceptance only.

Not examined: `launch(threads, block)` with blocks that are not whole warps, zero or symbolic, or called from a lane or a cooperative thread; one symbol defined by two vendored sources, a `.cu` source in a host build, and injection through extern names beyond reading; `unbuildable` for PTX of another architecture and for host assembly of another family; the `E-COOP-GLOBAL` weight rule beyond two attacks, and the `d799762` relaxation, which was read and not run (PROOFS found two radix holes in `footprints.py` while modelling it, fixed in `7ab0561`); warp operations under split conditions beyond the fragment cases; shared arrays declared in loops and conditions; the execution contexts and scratch reuse of `cairn_exec.hpp` and `cairn_reuse.hpp`; the device target beyond reading `projects/target.py`; implementation-session requests beyond the submission checks; routes to a reference through `dyn` or a trait method.

Two answers are stated as they are and not changed. A NaN agrees with any NaN in validation, whatever its sign and payload, and `docs/tools.md` now says so. A validated answer can carry a Z3 counterexample beside a finite pass, since the Z3 answer is reported apart; failing on one, or replaying it as a case, would close that gap. An implementation session keys its validations without the vendored-source digests, so `cairn tune` does not cite a session's validation of a foreign implementation: stale in the safe direction.
