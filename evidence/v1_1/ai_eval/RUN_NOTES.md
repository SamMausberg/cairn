# Run notes

Every event of the counted run that is not a subject's own result, in order. Times are the machine's local time (UTC-7) on 2026-09-24.

- 12:16:34. The preregistration reached main as dd3f75e (pull request #53). `harness.py verify` at dd3f75e showed 91 of 91 as required (`verify.json`).
- 12:23:28. The counted run started, three subjects at a time, with the toolchain, documentation and plugin built from dd3f75e (`toolchain_commit` in every record).
- In the first twelve subjects (the first three tasks in all four arms), three CAIRN subjects searched the whole file system: the `histogram` plugin subject for `docs` folders, and the `histogram` and `chunk_sums` documentation subjects for CAIRN's library and Python sources. None read a file outside what it was given (`audit_decisions.json`). But the preregistered audit did not see a search from `/`, and nothing kept a subject from the repository's checkouts or from a concurrent subject's sandbox.
- 12:39:39. A PAUSE file stopped new subjects, and the run stopped at 12:43:53 with those twelve recorded. The harness changed (cdaf81e): each subject now runs in a user and mount namespace where it sees its own `/tmp`, sandbox and plugin copy, and empty folders at every checkout of the repository, at the run's other sandboxes, plugin copies and scratch folders, and at `~/.claude/projects`. Each record also names the harness commit and the load average at its subject's start and end. The audit gained two flags, a walk of the file system from `/` and a tool result that names another checkout, another subject's folder or a task's reference; the report audits every transcript under this final rule. 12:44:04, the run resumed.
- In `sieve`, the documentation subject listed the machine's processes and killed the `find` processes it saw, two of them the concurrent plugin subject's. 13:18:50, a second pause; the run stopped at 13:24:21 with 50 subjects recorded. The harness changed again (405377c): each session also gets a process namespace, so it sees and signals only its own processes and whatever it leaves running ends with it. 13:24:36, the run resumed.
- 13:25:32, a third pause, for a harness bug. The plugin arm's copy of the plugin lived in a folder per task, and every arm's cleanup removed it, so when a faster arm of the same task was judged, a running plugin subject lost its skill's cards, its documentation and its `bin/cairn` mid-session. Eleven of the thirteen replicate 1 plugin subjects lost it, from 2 to 396 seconds into their sessions; the `rle` and `tally` plugin subjects kept it throughout, and no other arm was affected. The replicate 2 `histogram` plugin subject was running; its copy was renamed on the host so the old cleanup could not reach it, and its session kept it. The run stopped at 13:32:00. The fix (546f182) gives only the plugin arm a copy. The eleven affected subjects were set aside, kept on file as `harness-0.json` with their transcripts and programs and reported apart in `tables_counted.md`, and their cells ran again with fresh subjects, which the preregistration allows only for a platform failure. The criterion was the timestamps alone, and all eleven had solved their tasks. 13:32:35, the run resumed with those reruns.
- 13:43:03. At the owner's request the session ended: a PAUSE file stopped new subjects, the subjects in flight finished and were judged, and the run stands where it stopped. `harness.py run --phase counted --replicate N` continues it from the first cell without a record, under the same toolchain in `~/cairn-aieval/toolchain`, once the PAUSE file in `results/ai_eval/` is removed.
- The machine was heavily loaded throughout: other agents ran test suites and CI reproductions beside the run, and load averages from 78 to 251 on 16 threads were seen. The records from `pool` in replicate 1 on name their load averages. Wall time is the least comparable measure; tokens, turns and cost do not depend on load.
