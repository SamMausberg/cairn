# Run notes

Every event of the counted run that is not a subject's own result, in order. Times are the machine's local time on 2026-09-23.

- The primary run started at b6e53c0, the commit that holds the preregistration, with the toolchain and documentation snapshot built from it (`toolchain/COMMIT`).
- After the first CAIRN subject (histogram), the audit flagged two reads under `~/.claude/projects/<sandbox>/tool-results/`. That is where the platform saves a long tool output of the session itself, and it hands the subject that path. The subject read its own build output back. The audit rule now allows that one directory, named after the subject's own sandbox. The report runs the audit again over every transcript under the rule as it stands, so every subject is audited alike.
- After chunk_sums in CAIRN finished, the harness stopped on an audit bug: a transcript line whose `message` was a string. The session had ended normally (`success`, 63 turns), and its transcript and program were already saved. The subject was judged afterwards from those files with the same toolchain (`recovered` in its record) and was not rerun. The harness's own clock was lost for that subject, so its wall time is the platform's `duration_ms`. The same fix stops the path pattern from reading `j*n//k` as a path.
- The captain paused the run between subjects for an owner's device window, after chunk_sums. No subject was running, and none started during the pause. The window was postponed, and the run resumed with records in replicate 1.
- During records in replicate 1 the captain asked for a second pause for a device window, and withdrew it before the running subject finished. No subject was skipped or delayed. From then on the owner's device tests ran beside the benchmark in a separate checkout, so wall times after that point were measured on a busier machine. Tokens, turns and cost do not depend on machine load.
- Another agent changed the audit's path pattern (7c67c72) so that a path written with a doubled leading slash, such as `//etc/passwd`, is still read as a path. It audited the sixteen transcripts then recorded under the new rule, and none gained a flag. The report audits every transcript again under this final rule.
- The primary run finished with all sixty cells. The chain then started the secondary run on `claude-opus-5-5`. The owner asked the campaign to wrap up and keep usage low, so the secondary run was stopped. `histogram` in CAIRN had finished and been judged. `histogram` in C++ was stopped after 28 seconds, before its closing record; the harness filed it as an infrastructure failure, and it was not rerun. No other secondary cell started.
