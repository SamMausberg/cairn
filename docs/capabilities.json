{
  "profile": "cairn-native/1.0.0",
  "complete_cpp_breadth": false,
  "entire_compiler_proved": false,
  "lean_verified": "collector certificate checker, its 17 certificates and the collector loop model only",
  "model_proficiency_measured": false,
  "implemented": {
    "native_cpu": "Linux x86-64 and AArch64, C++20 lowering, libraries and executables, clang++ and g++",
    "scalars": "fixed integers (8..64 bit), bool, strict floating point, byte text and character literals",
    "types": "generic records and sums with any value payload, tag-only enums, inline arrays, layout attributes",
    "generics": "type and natural parameters, inference, on-demand monomorphization, bounded families",
    "traits": "static dispatch on Self, bounds checked per instance, methods by receiver module, borrowed dyn references",
    "memory": "second-class borrows of values and arrays, checked parts, scoped and first-class zeroed owners",
    "ownership": "affine moves, take/swap, linear structs, defer, implicit release visible as effects",
    "errors": "typed sums with exhaustive match and explicit try propagation",
    "functions": "code pointers and non-escaping closures as borrowed callables",
    "effects": "interprocedural rows with renamed footprints, pure/effects ceilings, mandatory extern effects, unsafe gate",
    "concurrency": "scoped tasks with linear tickets and borrow leases, atomics with explicit orders, closure-entered mutexes",
    "parallel": "race-free lane regions on host threads and CUDA lanes from one body, reductions, device collector",
    "placement": "host, pinned, unified and device views in the type; transfer is the only crossing",
    "modules": "namespaces, pub, imports with aliases and name lists, a packaged std written in CAIRN",
    "targets": "hosted, CUDA through nvcc, and a freestanding AArch64 profile with MMIO and inline target instructions",
    "generation": "bounded natural families, fixed unsigned wire codecs, the bounded collector",
    "projects": "ordered files, data-only manifests, fresh build directories",
    "editing": "host-bound body/expression choices over the whole language, lexical rule cards, effect ceilings",
    "tooling": "comment-preserving formatter, language server, editor grammar",
    "arithmetic_certificates": "17 exact affine implications, checked before every emission and proved in Lean",
    "scalar_equivalence": "trusted translator and Z3, explicit per-function/module coverage"
  },
  "not_implemented": [
    "library-defined typed recipes (general contracted expansion)",
    "owned dynamic values (boxed dyn)",
    "asynchronous I/O with completion tickets",
    "multi-device collectives",
    "checking a generic once against its bounds rather than per instance",
    "per-module objects with interface digests",
    "SMT coverage of memory, loops, sums and floats",
    "emitter-to-model and native refinement proofs",
    "ownership and lease calculus mechanization",
    "measured AI proficiency"
  ],
  "principle": "Implemented, finite-tested, sanitizer-clean, exact-algebra-checked, SMT-equivalent and Lean-checked are distinct scopes."
}
